# ServerHUB · Known Issues

> Bug / 已识别问题清单。bug 修复后从这里删除，并评估是否需要把教训沉淀到 `systemPatterns.md`。

暂无已识别的运行时 bug。

> 提示：当前在 `Current Focus`（SSH 一键部署 + Dashboard UI 升级）阶段，对应的 untracked / modified 改动属于「未完工」而非「bug」，统一在 `progress.md` 跟踪，不写到本文件。
